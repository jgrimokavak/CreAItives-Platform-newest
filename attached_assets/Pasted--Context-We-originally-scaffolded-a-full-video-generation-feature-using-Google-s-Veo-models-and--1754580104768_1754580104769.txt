# Context
# We originally scaffolded a full video‐generation feature using Google’s Veo models and Vertex AI,
# but:
# 1. All Vertex calls were just placeholders—no real jobs ever ran.
# 2. The UI and server code have become stale and misaligned with our new requirements.
# 3. Keeping dead code creates unnecessary maintenance overhead and confusion.
# To start clean and integrate a different video engine, we need to remove every trace of the old setup.

Objective: Remove all existing video generation functionality from the repository so we can start fresh.

1. Identify and delete front-end video generation components  
   - Remove pages:  
     ```ts
     // client/src/pages/VideoCreationPage.tsx
     // client/src/pages/VideoCreationPageNew.tsx
     rm client/src/pages/VideoCreationPage.tsx client/src/pages/VideoCreationPageNew.tsx
     ```  
   - Delete component:  
     ```ts
     // client/src/components/VideoProjectGallery.tsx
     rm client/src/components/VideoProjectGallery.tsx
     ```  
   - Update model catalog:  
     ```ts
     // client/src/lib/modelCatalog.ts
     // Remove any entries for 'Veo' models
     ```  

2. Remove back-end video routes and logic  
   - Delete route file:  
     ```ts
     // server/routes/video-routes.ts
     rm server/routes/video-routes.ts
     ```  
   - Unregister in app entry:  
     ```ts
     // server/app.ts (or index.ts)
     - import videoRoutes from './routes/video-routes';
     - app.use('/video', videoRoutes);
     ```  
   - Remove Vertex integration:  
     ```ts
     // server/vertex.ts
     rm server/vertex.ts
     ```  

3. Purge storage and schema entries  
   - Remove video schemas:  
     ```ts
     // shared/schema.ts
     // Delete Zod schema definitions for video generation
     ```  
   - Clean storage methods:  
     ```ts
     // server/storage.ts
     // Remove createVideo, getVideoById, updateVideo, getVideosByProject
     ```  
   - Drop DB migrations:  
     ```
     // migrations/YYYYMMDD_create_videos_table.ts
     rm migrations/*create_videos_table*.ts
     ```  

4. Clean up configuration and dependencies  
   - Delete credentials file:  
     ```bash
     rm credentials/video-generator-key.json
     ```  
   - Remove env vars from `.env`:  
     ```
     GOOGLE_APPLICATION_CREDENTIALS=
     GOOGLE_PROJECT_ID=
     GCP_LOCATION=
     GCS_BUCKET=
     ```  
   - Uninstall Vertex SDK:  
     ```bash
     npm uninstall @google-cloud/aiplatform
     npm prune
     ```  

5. Verify removal and commit  
   - Confirm deletions:  
     ```bash
     git status
     ```  
   - Commit changes:  
     ```bash
     git add .
     git commit -m "chore: purge video generation"
     ```

Testing:
- **Scenario 1:** Run `npm start` and confirm no build errors referencing video files.  
- **Scenario 2:** Navigate to `/video` in the browser; expect a 404 or no UI.  
- **Scenario 3:** Search codebase for “Veo” or “Vertex”; ensure zero matches.

Reminders & Constraints:
- Do not modify unrelated features or routes.  
- Preserve Git history; avoid force-deleting branches.  
- Use TypeScript code fences (` ```ts `) for any code examples.  
- Execute all commands from the repository root.
